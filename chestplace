--this will get the item detail thats in the turtles inventory.
turtle.select(1)
local iItem = turtle.getItemDetail(1) --this saves the id, count, and damage count (uselss) of the block
--this is a list of the items acceptable to our bank and their values.
--"minecraft:iron_ingot" = x
 x = 0.05
--"minecraft:gold_nugget" = y
 y = 0.25
--"minecraft:gold_ingot" = a
 a = 2.50
--"minecraft:iron_block" = b
 b = 3.00
--"minecraft:gold_block" = c
 c = 25.00
--"minecraft:diamond" = d
 d = 50.00
--"minecraft:diamond_block" = h
 h = 450.00
--"minecraft:emerald" = e
 e = 100.00
--"minecraft:emerald_block" = f
 f = 900.00
 
turtle.turnRight()
--this is to pick up the dropped items.
turtle.suck()
turtle.turnLeft()

iItem = turtle.getItemDetail(1) 
if iItem.name == "minecraft:diamond" then
--this is for diamond that the clients want to deposit  if iItem.name == "minecraft:diamond" then
--this is to transfer items to a certain slot
 turtle.up()
 turlte.down()
 turtle.transferTo(6)
 turtle.up()
 turtle.down() 
--this is to get the amou nt of money the client has put in in diamonds
  itemValue = 4 * d
   rednet.open("left")
--this is to send the value of the items back to the computer with a certain code
    rednet.broadcast(itemValue,"467")
    rednet.close("left")
    turtle.select(6)
--this is to drop the items into the chest     
      turtle.drop()
      turtle.up()
 --     print("Ducats")
 
--this is for diamond blocks that the clients want to deposit
  elseif iItem.name == "minecraft:diamond_block" then
   turtle.transferTo(7)
    itemValue = iItem.count*h
     rednet.open("left")
      rednet.broadcast(itemValue,"467")
       rednet.close("left")
      turtle.select(7)
       turtlel.drop()
 
 --this is for the gold ingots that the clients want to deposit
  elseif iItem.name == "minecraft:gold_ingot" then
   turtle.transferTo(3)
    itemValue = iItem.count*a
     rednet.open("left")
      rednet.broadcast(itemValue,"467")
       rednet.close("left")
     turtle.select(3)
      turtle.drop()
 
--this is for the gold blocks that the clients want to deposit
 elseif iItem.name == "minecraft:gold_block" then
  turtle.transferTo(5)
   itemValue = iItem.count*c
    rednet.open("left")
     rednet.bradcast(itemValue,"659")
      rednet.close("left")
    turtle.select(5)
     turtle.drop()

 --this is for the gold nuggets that the clients want to deposit
   elseif iItem.name == "minecraft:gold_nugget" then
     turtle.transferTo(2)
       itemValue = iItem.count*y
          rednet.open("left")
            rednet.bradcast(itemValue,"658")
              rednet.close("left")
          turtle.select(2)
            turtle.drop()

--this is for the emeralds thhat the clients want to deposit
 elseif iItem.name == "minecraft:emerald"then
  turtle.transferTo(8)
   itemValue = iItem.count*e
    rednet.open("left")
     rednet.broadcast(itemValue,"467")
      rednet.close("left")
       turtle.select(8)
     turtle.drop()

--this is for the emerald blocks that the client wants to deposit
 elseif iItem.name == "minecraft:emerald_block" then
  turle.transferTo(9)
   itemValue = iItem.count*f
    rednet.open("left")
     rednet.broadcast(itemValue,"467")
      rednet.close("left")
     turtle.select(9)  
      turtle.drop()
 
--this is for the iron ingots that the client wants to deposit
 elseif iItem.name == "minecraft:iron_ingot" then
 turtle.transferTo(1)
   itemValue = iItem.count*x
    rednet.open("left")
     rednet.broadcast(itemValue,"333")
      rednet.close("left")
    turle.select(1)  
     turtle.drop()
 
--this is for the iron blocks that the client wants to deposit
 elseif iItem.name == "minecraft:iron_block" then
  turtle.transferTo(4)
   itemValue = iItem.count*b
    rednet.open("left")
     rednet.broadcast(itemValue,"467")
      rednet.close("left")
    turtle.select(4)   
     turtle.drop()

--this is to reject the items that arent compatible for our bank
 else
   turtle.turnRight()
   turtle.drop()
   turtle.turnLeft()
 
end
 
turtle.turnRight()
